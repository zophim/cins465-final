<%= form_for(@op) do |f| %>
  <% if @op.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@op.errors.count, "error") %> prohibited this op from being saved:</h2>

      <ul>
      <% @op.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :forum_id %><br />
    <%= f.number_field :forum_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
